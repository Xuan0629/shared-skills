=== COMMAND ===
codex exec --dangerously-bypass-approvals-and-sandbox Review Iteration 1: 1. Run tests: python3 sync-skills.py --check 2>&1 | head -5 2. Write review to /home/sean/shared-skills/reviews/iter-1.md 3. Use YAML frontmatter: verdict, summary, findings, metrics 4. Do NOT modify src/

# Reviewer
Review developer output. Run test command. Write to reviews/iter-N.md:
```yaml
---
verdict: PASS
summary: "one-line"
findings:
  - severity: blocker|major|minor
    category: correctness|security|completeness
    summary: "brief"
```


## Phase Status
mode: code-dev, phase: dev, iteration: 1/3, prev_verdict: N/A, budget_remaining: 499717 tokens

## Git Diff
diff --git a/.unison/pipelines/p0.yaml b/.unison/pipelines/p0.yaml
new file mode 100644
index 0000000..86cbcfc
--- /dev/null
+++ b/.unison/pipelines/p0.yaml
@@ -0,0 +1,13 @@
+version: "2.0"
+project_root: "/home/sean/shared-skills"
+mode: "code-dev"
+agents:
+  developer: {role: developer, runtime: claude, model: claude-sonnet-4-6, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/dev-p0.md", pipeline_role: developer}
+  reviewer:  {role: reviewer,  runtime: codex, model: gpt-5.5, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/review-generic.md", pipeline_role: reviewer}
+project:
+  test_command: "python3 -c "import yaml; m=yaml.safe_load(open('skills-manifest.yaml')); assert all(k in m for k in ['planning-with-files','design-system']); print('OK')""
+  max_iterations: 3
+  per_agent_timeout: 600
+budget:
+  daily_token_limit: 5000000
+  per_task_limit: 500000
diff --git a/.unison/pipelines/p1.yaml b/.unison/pipelines/p1.yaml
new file mode 100644
index 0000000..a78ed5f
--- /dev/null
+++ b/.unison/pipelines/p1.yaml
@@ -0,0 +1,13 @@
+version: "2.0"
+project_root: "/home/sean/shared-skills"
+mode: "code-dev"
+agents:
+  developer: {role: developer, runtime: claude, model: claude-sonnet-4-6, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/dev-p1.md", pipeline_role: developer}
+  reviewer:  {role: reviewer,  runtime: codex, model: gpt-5.5, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/review-generic.md", pipeline_role: reviewer}
+project:
+  test_command: "python3 -c "import os,yaml; [print(f'{d}: OK') for d in os.listdir('.') if os.path.isdir(d) and not d.startswith('.') and os.path.exists(f'{d}/meta.yaml')]""
+  max_iterations: 3
+  per_agent_timeout: 600
+budget:
+  daily_token_limit: 5000000
+  per_task_limit: 500000
diff --git a/.unison/pipelines/p2.yaml b/.unison/pipelines/p2.yaml
new file mode 100644
index 0000000..bf8798a
--- /dev/null
+++ b/.unison/pipelines/p2.yaml
@@ -0,0 +1,13 @@
+version: "2.0"
+project_root: "/home/sean/shared-skills"
+mode: "code-dev"
+agents:
+  developer: {role: developer, runtime: claude, model: claude-sonnet-4-6, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/dev-p2.md", pipeline_role: developer}
+  reviewer:  {role: reviewer,  runtime: codex, model: gpt-5.5, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/review-generic.md", pipeline_role: reviewer}
+project:
+  test_command: "python3 sync-skills.py --check 2>&1 | head -5"
+  max_iterations: 3
+  per_agent_timeout: 600
+budget:
+  daily_token_limit: 5000000
+  per_task_limit: 500000
diff --git a/.unison/pipelines/p3.yaml b/.unison/pipelines/p3.yaml
new file mode 100644
index 0000000..c724c94
--- /dev/null
+++ b/.unison/pipelines/p3.yaml
@@ -0,0 +1,13 @@
+version: "2.0"
+project_root: "/home/sean/shared-skills"
+mode: "code-dev"
+agents:
+  developer: {role: developer, runtime: claude, model: claude-sonnet-4-6, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/dev-p3.md", pipeline_role: developer}
+  reviewer:  {role: reviewer,  runtime: codex, model: gpt-5.5, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/review-generic.md", pipeline_role: reviewer}
+project:
+  test_command: "python3 -c "import json; [print(json.loads(l)['skill']) for l in open('sync.log') if l.strip()]" 2>/dev/null || python3 -c "print('sync log will be created on first sync')""
+  max_iterations: 3
+  per_agent_timeout: 600
+budget:
+  daily_token_limit: 5000000
+  per_task_limit: 500000
diff --git a/.unison/prompts/dev-p0.md b/.unison/prompts/dev-p0.md
new file mode 100644
index 0000000..25f3e5f
--- /dev/null
+++ b/.unison/prompts/dev-p0.md
@@ -0,0 +1,13 @@
+# P0: Fix OpenClaw Bug + Schema Validation
+
+## Bug: OpenClaw description reads from codex
+In sync-skills.py, find `build_openclaw_frontmatter()`. It reads description from `meta.get("codex", ...)` instead of `meta.get("openclaw", ...)`. Fix it.
+
+## Add: Manifest Schema Validation
+Add a `validate_manifest()` function that checks:
+- Every skill name in manifest has a corresponding directory with SKILL.md
+- Every agent name is one of: hermes, claude, codex, openclaw
+- Every skill has at least one trigger/description
+- Raise clear errors (not silent skip) on invalid entries
+
+Run test command. Commit fixes.
\ No newline at end of file
diff --git a/.unison/prompts/dev-p1.md b/.unison/prompts/dev-p1.md
new file mode 100644
index 0000000..3c81e68
--- /dev/null
+++ b/.unison/prompts/dev-p1.md
@@ -0,0 +1,37 @@
+# P1: Manifest Split — meta.yaml Per Skill
+
+## Goal
+Move agent metadata from the monolithic `skills-manifest.yaml` into per-skill `meta.yaml` files.
+
+## New Structure
+```
+~/shared-skills/planning-with-files/
+  SKILL.md
+  meta.yaml          ← NEW: agent metadata, version, sync_to list
+  references/
+  scripts/
+
+~/shared-skills/skills-manifest.yaml  ← degraded to index: [skill1, skill2, ...]
+```
+
+## meta.yaml Format
+```yaml
+version: "1.0.0"
+sync_to: [hermes, claude, codex]
+hermes:
+  category: productivity
+  triggers: ["plan with files", "制定计划"]
+claude:
+  description: "Crash-proof persistent file-based planning"
+  allowed-tools: [Bash, Read, Glob]
+codex:
+  description: "Crash-proof persistent file-based planning"
+```
+
+## Implementation
+1. Create `meta.yaml` in each of the 8 skill directories with existing metadata from skills-manifest.yaml
+2. Update `skills-manifest.yaml` to be a simple list: `[planning-with-files, design-system, ...]`
+3. Update `sync-skills.py` to read from per-skill meta.yaml instead of the big manifest
+4. Add `sync_to` field support — only sync to agents listed in sync_to
+
+Run test command. Commit.
\ No newline at end of file
diff --git a/.unison/prompts/dev-p2.md b/.unison/prompts/dev-p2.md
new file mode 100644
index 0000000..01d9b1f
--- /dev/null
+++ b/.unison/prompts/dev-p2.md
@@ -0,0 +1,24 @@
+# P2: Source Hash + --check Mode
+
+## Goal
+Track skill versions and detect when agent copies are outdated.
+
+## Implementation
+1. In sync-skills.py, before writing a skill to an agent directory:
+   - Compute SHA256 of (SKILL.md + meta.yaml)
+   - Write `source_hash: <sha256>` into the generated frontmatter
+2. Add `--check` CLI argument:
+   - Scan each agent's skill directory
+   - Compare stored source_hash against current shared source
+   - Report: "UP-TO-DATE", "OUTDATED", or "MISSING" for each skill-agent pair
+3. Add `--verbose` flag to show hash values
+
+Example output:
+```
+planning-with-files:
+  hermes: OUTDATED (abc123 vs def456)
+  claude: UP-TO-DATE
+  codex: MISSING
+```
+
+Run test and commit.
\ No newline at end of file
diff --git a/.unison/prompts/dev-p3.md b/.unison/prompts/dev-p3.md
new file mode 100644
index 0000000..1db8992
--- /dev/null
+++ b/.unison/prompts/dev-p3.md
@@ -0,0 +1,15 @@
+# P3: Sync Log
+
+## Goal
+Record every sync operation for audit trail.
+
+## Implementation
+1. Create `sync.log` in `~/shared-skills/` (JSON Lines format)
+2. For each skill synced, append a line:
+   ```json
+   {"timestamp":"2026-07-06T12:00:00Z","skill":"planning-with-files","agent":"hermes","action":"updated","hash_before":"abc123","hash_after":"def456"}
+   ```
+3. Log actions: "created", "updated", "skipped" (unchanged), "removed"
+4. Add `--show-log` CLI flag to display recent sync history
+
+Run test and commit.
\ No newline at end of file
diff --git a/.unison/prompts/review-generic.md b/.unison/prompts/review-generic.md
new file mode 100644
index 0000000..83bc2b1
--- /dev/null
+++ b/.unison/prompts/review-generic.md
@@ -0,0 +1,11 @@
+# Reviewer
+Review developer output. Run test command. Write to reviews/iter-N.md:
+```yaml
+---
+verdict: PASS
+summary: "one-line"
+findings:
+  - severity: blocker|major|minor
+    category: correctness|security|completeness
+    summary: "brief"
+```
diff --git a/.unison/run.log b/.unison/run.log
new file mode 100644
index 0000000..23ce744
--- /dev/null
+++ b/.unison/run.log
@@ -0,0 +1,113 @@
+
+=== p0 2026年 07月 06日 星期一 02:04:03 HKT ===
+Traceback (most recent call last):
+  File "/home/sean/.local/bin/unison", line 6, in <module>
+    sys.exit(main())
+             ^^^^^^
+  File "/home/sean/projects/unison/sr
...[diff truncated]

## Cumulative Changes (since loop start)
 .unison/pipelines/p0.yaml                          |  13 ++
 .unison/pipelines/p1.yaml                          |  13 ++
 .unison/pipelines/p2.yaml                          |  13 ++
 .unison/pipelines/p3.yaml                          |  13 ++
 .unison/prompts/dev-p0.md                          |  13 ++
 .unison/prompts/dev-p1.md                          |  37 ++++++
 .unison/prompts/dev-p2.md                          |  24 ++++
 .unison/prompts/dev-p3.md                          |  15 +++
 .unison/prompts/review-generic.md                  |  11 ++
 .unison/run.log                                    | 113 ++++++++++++++++
 .../logs/developer_iter-1_20260705T180405Z.log     |  36 +++++
 reviews/acceptance-criteria.md                     |  17 +++
 sync-skills.py                                     | 147 ++++++++++++++++++---
 13 files changed, 450 insertions(+), 15 deletions(-)


=== OUTPUT ===
Reading additional input from stdin...
OpenAI Codex v0.142.5
--------
workdir: /home/sean/shared-skills
model: gpt-5.5
provider: openai-custom
approval: never
sandbox: danger-full-access
reasoning effort: high
reasoning summaries: none
session id: 019f3377-05dc-7910-a010-7cf6f87e9d41
--------
user
Review Iteration 1: 1. Run tests: python3 sync-skills.py --check 2>&1 | head -5 2. Write review to /home/sean/shared-skills/reviews/iter-1.md 3. Use YAML frontmatter: verdict, summary, findings, metrics 4. Do NOT modify src/

# Reviewer
Review developer output. Run test command. Write to reviews/iter-N.md:
```yaml
---
verdict: PASS
summary: "one-line"
findings:
  - severity: blocker|major|minor
    category: correctness|security|completeness
    summary: "brief"
```


## Phase Status
mode: code-dev, phase: dev, iteration: 1/3, prev_verdict: N/A, budget_remaining: 499717 tokens

## Git Diff
diff --git a/.unison/pipelines/p0.yaml b/.unison/pipelines/p0.yaml
new file mode 100644
index 0000000..86cbcfc
--- /dev/null
+++ b/.unison/pipelines/p0.yaml
@@ -0,0 +1,13 @@
+version: "2.0"
+project_root: "/home/sean/shared-skills"
+mode: "code-dev"
+agents:
+  developer: {role: developer, runtime: claude, model: claude-sonnet-4-6, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/dev-p0.md", pipeline_role: developer}
+  reviewer:  {role: reviewer,  runtime: codex, model: gpt-5.5, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/review-generic.md", pipeline_role: reviewer}
+project:
+  test_command: "python3 -c "import yaml; m=yaml.safe_load(open('skills-manifest.yaml')); assert all(k in m for k in ['planning-with-files','design-system']); print('OK')""
+  max_iterations: 3
+  per_agent_timeout: 600
+budget:
+  daily_token_limit: 5000000
+  per_task_limit: 500000
diff --git a/.unison/pipelines/p1.yaml b/.unison/pipelines/p1.yaml
new file mode 100644
index 0000000..a78ed5f
--- /dev/null
+++ b/.unison/pipelines/p1.yaml
@@ -0,0 +1,13 @@
+version: "2.0"
+project_root: "/home/sean/shared-skills"
+mode: "code-dev"
+agents:
+  developer: {role: developer, runtime: claude, model: claude-sonnet-4-6, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/dev-p1.md", pipeline_role: developer}
+  reviewer:  {role: reviewer,  runtime: codex, model: gpt-5.5, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/review-generic.md", pipeline_role: reviewer}
+project:
+  test_command: "python3 -c "import os,yaml; [print(f'{d}: OK') for d in os.listdir('.') if os.path.isdir(d) and not d.startswith('.') and os.path.exists(f'{d}/meta.yaml')]""
+  max_iterations: 3
+  per_agent_timeout: 600
+budget:
+  daily_token_limit: 5000000
+  per_task_limit: 500000
diff --git a/.unison/pipelines/p2.yaml b/.unison/pipelines/p2.yaml
new file mode 100644
index 0000000..bf8798a
--- /dev/null
+++ b/.unison/pipelines/p2.yaml
@@ -0,0 +1,13 @@
+version: "2.0"
+project_root: "/home/sean/shared-skills"
+mode: "code-dev"
+agents:
+  developer: {role: developer, runtime: claude, model: claude-sonnet-4-6, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/dev-p2.md", pipeline_role: developer}
+  reviewer:  {role: reviewer,  runtime: codex, model: gpt-5.5, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/review-generic.md", pipeline_role: reviewer}
+project:
+  test_command: "python3 sync-skills.py --check 2>&1 | head -5"
+  max_iterations: 3
+  per_agent_timeout: 600
+budget:
+  daily_token_limit: 5000000
+  per_task_limit: 500000
diff --git a/.unison/pipelines/p3.yaml b/.unison/pipelines/p3.yaml
new file mode 100644
index 0000000..c724c94
--- /dev/null
+++ b/.unison/pipelines/p3.yaml
@@ -0,0 +1,13 @@
+version: "2.0"
+project_root: "/home/sean/shared-skills"
+mode: "code-dev"
+agents:
+  developer: {role: developer, runtime: claude, model: claude-sonnet-4-6, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/dev-p3.md", pipeline_role: developer}
+  reviewer:  {role: reviewer,  runtime: codex, model: gpt-5.5, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/review-generic.md", pipeline_role: reviewer}
+project:
+  test_command: "python3 -c "import json; [print(json.loads(l)['skill']) for l in open('sync.log') if l.strip()]" 2>/dev/null || python3 -c "print('sync log will be created on first sync')""
+  max_iterations: 3
+  per_agent_timeout: 600
+budget:
+  daily_token_limit: 5000000
+  per_task_limit: 500000
diff --git a/.unison/prompts/dev-p0.md b/.unison/prompts/dev-p0.md
new file mode 100644
index 0000000..25f3e5f
--- /dev/null
+++ b/.unison/prompts/dev-p0.md
@@ -0,0 +1,13 @@
+# P0: Fix OpenClaw Bug + Schema Validation
+
+## Bug: OpenClaw description reads from codex
+In sync-skills.py, find `build_openclaw_frontmatter()`. It reads description from `meta.get("codex", ...)` instead of `meta.get("openclaw", ...)`. Fix it.
+
+## Add: Manifest Schema Validation
+Add a `validate_manifest()` function that checks:
+- Every skill name in manifest has a corresponding directory with SKILL.md
+- Every agent name is one of: hermes, claude, codex, openclaw
+- Every skill has at least one trigger/description
+- Raise clear errors (not silent skip) on invalid entries
+
+Run test command. Commit fixes.
\ No newline at end of file
diff --git a/.unison/prompts/dev-p1.md b/.unison/prompts/dev-p1.md
new file mode 100644
index 0000000..3c81e68
--- /dev/null
+++ b/.unison/prompts/dev-p1.md
@@ -0,0 +1,37 @@
+# P1: Manifest Split — meta.yaml Per Skill
+
+## Goal
+Move agent metadata from the monolithic `skills-manifest.yaml` into per-skill `meta.yaml` files.
+
+## New Structure
+```
+~/shared-skills/planning-with-files/
+  SKILL.md
+  meta.yaml          ← NEW: agent metadata, version, sync_to list
+  references/
+  scripts/
+
+~/shared-skills/skills-manifest.yaml  ← degraded to index: [skill1, skill2, ...]
+```
+
+## meta.yaml Format
+```yaml
+version: "1.0.0"
+sync_to: [hermes, claude, codex]
+hermes:
+  category: productivity
+  triggers: ["plan with files", "制定计划"]
+claude:
+  description: "Crash-proof persistent file-based planning"
+  allowed-tools: [Bash, Read, Glob]
+codex:
+  description: "Crash-proof persistent file-based planning"
+```
+
+## Implementation
+1. Create `meta.yaml` in each of the 8 skill directories with existing metadata from skills-manifest.yaml
+2. Update `skills-manifest.yaml` to be a simple list: `[planning-with-files, design-system, ...]`
+3. Update `sync-skills.py` to read from per-skill meta.yaml instead of the big manifest
+4. Add `sync_to` field support — only sync to agents listed in sync_to
+
+Run test command. Commit.
\ No newline at end of file
diff --git a/.unison/prompts/dev-p2.md b/.unison/prompts/dev-p2.md
new file mode 100644
index 0000000..01d9b1f
--- /dev/null
+++ b/.unison/prompts/dev-p2.md
@@ -0,0 +1,24 @@
+# P2: Source Hash + --check Mode
+
+## Goal
+Track skill versions and detect when agent copies are outdated.
+
+## Implementation
+1. In sync-skills.py, before writing a skill to an agent directory:
+   - Compute SHA256 of (SKILL.md + meta.yaml)
+   - Write `source_hash: <sha256>` into the generated frontmatter
+2. Add `--check` CLI argument:
+   - Scan each agent's skill directory
+   - Compare stored source_hash against current shared source
+   - Report: "UP-TO-DATE", "OUTDATED", or "MISSING" for each skill-agent pair
+3. Add `--verbose` flag to show hash values
+
+Example output:
+```
+planning-with-files:
+  hermes: OUTDATED (abc123 vs def456)
+  claude: UP-TO-DATE
+  codex: MISSING
+```
+
+Run test and commit.
\ No newline at end of file
diff --git a/.unison/prompts/dev-p3.md b/.unison/prompts/dev-p3.md
new file mode 100644
index 0000000..1db8992
--- /dev/null
+++ b/.unison/prompts/dev-p3.md
@@ -0,0 +1,15 @@
+# P3: Sync Log
+
+## Goal
+Record every sync operation for audit trail.
+
+## Implementation
+1. Create `sync.log` in `~/shared-skills/` (JSON Lines format)
+2. For each skill synced, append a line:
+   ```json
+   {"timestamp":"2026-07-06T12:00:00Z","skill":"planning-with-files","agent":"hermes","action":"updated","hash_before":"abc123","hash_after":"def456"}
+   ```
+3. Log actions: "created", "updated", "skipped" (unchanged), "removed"
+4. Add `--show-log` CLI flag to display recent sync history
+
+Run test and commit.
\ No newline at end of file
diff --git a/.unison/prompts/review-generic.md b/.unison/prompts/review-generic.md
new file mode 100644
index 0000000..83bc2b1
--- /dev/null
+++ b/.unison/prompts/review-generic.md
@@ -0,0 +1,11 @@
+# Reviewer
+Review developer output. Run test command. Write to reviews/iter-N.md:
+```yaml
+---
+verdict: PASS
+summary: "one-line"
+findings:
+  - severity: blocker|major|minor
+    category: correctness|security|completeness
+    summary: "brief"
+```
diff --git a/.unison/run.log b/.unison/run.log
new file mode 100644
index 0000000..23ce744
--- /dev/null
+++ b/.unison/run.log
@@ -0,0 +1,113 @@
+
+=== p0 2026年 07月 06日 星期一 02:04:03 HKT ===
+Traceback (most recent call last):
+  File "/home/sean/.local/bin/unison", line 6, in <module>
+    sys.exit(main())
+             ^^^^^^
+  File "/home/sean/projects/unison/sr
...[diff truncated]

## Cumulative Changes (since loop start)
 .unison/pipelines/p0.yaml                          |  13 ++
 .unison/pipelines/p1.yaml                          |  13 ++
 .unison/pipelines/p2.yaml                          |  13 ++
 .unison/pipelines/p3.yaml                          |  13 ++
 .unison/prompts/dev-p0.md                          |  13 ++
 .unison/prompts/dev-p1.md                          |  37 ++++++
 .unison/prompts/dev-p2.md                          |  24 ++++
 .unison/prompts/dev-p3.md                          |  15 +++
 .unison/prompts/review-generic.md                  |  11 ++
 .unison/run.log                                    | 113 ++++++++++++++++
 .../logs/developer_iter-1_20260705T180405Z.log     |  36 +++++
 reviews/acceptance-criteria.md                     |  17 +++
 sync-skills.py                                     | 147 ++++++++++++++++++---
 13 files changed, 450 insertions(+), 15 deletions(-)

codex
I’ll run the requested check first, then inspect the relevant implementation diff enough to make a reviewer call. I’ll only write the review artifact under `reviews/`.
exec
/bin/bash -lc 'rg --files' in /home/sean/shared-skills
 succeeded in 0ms:
sync-skills.py
planning-with-files/SKILL.md
skills-manifest.yaml
reviews/acceptance-criteria.md
diagnosing-bugs/SKILL.md
grill-design/SKILL.md
observer/logs/reviewer_iter-1_20260705T180742Z.log
observer/logs/developer_iter-1_20260705T180405Z.log
codebase-design/SKILL.md
handoff/SKILL.md
design-system/SKILL.md
role-play/SKILL.md
domain-modeling/SKILL.md

exec
/bin/bash -lc 'git diff -- sync-skills.py' in /home/sean/shared-skills
 succeeded in 0ms:
exec
/bin/bash -lc 'python3 sync-skills.py --check 2>&1 | head -5' in /home/sean/shared-skills
 succeeded in 431ms:
planning-with-files:
  hermes: UP-TO-DATE
  claude: UP-TO-DATE
  codex: UP-TO-DATE
  openclaw: UP-TO-DATE

codex
The requested command exits cleanly and the first five lines all report `UP-TO-DATE`. I’m checking the current file state now because the provided diff is truncated and `git diff -- sync-skills.py` is empty in the working tree.
exec
/bin/bash -lc 'git status --short' in /home/sean/shared-skills
 succeeded in 0ms:
 M observer/logs/developer_iter-1_20260705T180405Z.log
?? .unison/budget.json
?? observer/logs/reviewer_iter-1_20260705T180742Z.log

exec
/bin/bash -lc "sed -n '1,260p' sync-skills.py" in /home/sean/shared-skills
 succeeded in 2ms:
#!/usr/bin/env python3
"""sync-skills — 将 ~/shared-skills/ 同步到各 agent 的 skill 目录。

单向同步：共享源 → agent。修改必须从共享源开始。
用法: python3 sync-skills.sh [--dry-run] [skill-name]
"""

import hashlib
import os
import sys
import yaml
from pathlib import Path

SHARED = Path.home() / "shared-skills"
MANIFEST = SHARED / "skills-manifest.yaml"

AGENT_CONFIGS = {
    "hermes": {
        "base": Path.home() / ".hermes" / "skills",
        "frontmatter_fields": ["name", "description", "triggers"],
    },
    "claude": {
        "base": Path.home() / ".claude" / "skills",
        "frontmatter_fields": ["name", "description", "version", "allowed-tools"],
        "extra": {"version": "0.1.0", "allowed-tools": ["Bash", "Read", "Glob", "Grep"]},
    },
    "codex": {
        "base": Path.home() / ".codex" / "skills",
        "frontmatter_fields": ["name", "description"],
    },
    "openclaw": {
        "base": Path.home() / ".openclaw" / "agents" / "main" / "skills",
        "frontmatter_fields": ["name", "description"],
    },
}


def load_manifest():
    with open(MANIFEST) as f:
        return yaml.safe_load(f)


def read_body(skill_name: str) -> str:
    """读取共享 skill 的正文（去除空行首尾）。"""
    path = SHARED / skill_name / "SKILL.md"
    if not path.exists():
        raise FileNotFoundError(f"Missing shared SKILL.md: {path}")
    return path.read_text().strip()


def list_references(skill_name: str) -> list[Path]:
    """列出 references/ 下的文件。"""
    ref_dir = SHARED / skill_name / "references"
    if ref_dir.exists():
        return sorted(ref_dir.glob("*.md"))
    return []


def list_scripts(skill_name: str) -> list[Path]:
    """列出 scripts/ 下的文件。"""
    scripts_dir = SHARED / skill_name / "scripts"
    if scripts_dir.exists():
        return sorted(scripts_dir.glob("*"))
    return []


def compute_source_hash(skill_name: str, meta: dict) -> str:
    """Compute SHA256 hash of raw SKILL.md + serialized manifest metadata.

    This hash captures the full shared source for a skill — both its body
    and its agent-specific metadata (triggers, categories, descriptions).
    Any change to either will produce a different hash, which --check uses
    to detect outdated agent copies.
    """
    skill_path = SHARED / skill_name / "SKILL.md"
    skill_bytes = skill_path.read_bytes()
    meta_bytes = yaml.dump(meta, sort_keys=True, allow_unicode=True).encode("utf-8")
    combined = skill_bytes + b"\n---META---\n" + meta_bytes
    return hashlib.sha256(combined).hexdigest()


def parse_frontmatter(filepath: Path) -> dict | None:
    """Parse YAML frontmatter from a SKILL.md file.

    Returns the parsed dict on success, None if the file is missing,
    has no frontmatter, or the YAML is invalid.
    """
    if not filepath.exists():
        return None
    content = filepath.read_text()
    if not content.startswith("---"):
        return None
    end = content.find("---", 3)
    if end == -1:
        return None
    fm_text = content[3:end].strip()
    if not fm_text:
        return None
    try:
        parsed = yaml.safe_load(fm_text)
        return parsed if isinstance(parsed, dict) else None
    except yaml.YAMLError:
        return None


def build_hermes_frontmatter(skill_name: str, meta: dict, source_hash: str = "") -> str:
    """生成 Hermes 格式的 YAML frontmatter（含 triggers）。"""
    triggers = meta.get("hermes", {}).get("triggers", [])
    description = meta.get("claude", {}).get("description", skill_name)
    lines = ["---", f"name: {skill_name}", f"description: {description}"]
    if triggers:
        lines.append("triggers:")
        for t in triggers:
            # regex patterns with special chars need quoting
            if any(c in t for c in ".*{}[]()"):
                lines.append(f'  - "{t}"')
            else:
                lines.append(f"  - {t}")
    if source_hash:
        lines.append(f"source_hash: {source_hash}")
    lines.append("---")
    return "\n".join(lines)


def build_claude_frontmatter(skill_name: str, meta: dict, source_hash: str = "") -> str:
    """生成 Claude Code 格式的 YAML frontmatter。"""
    description = meta.get("claude", {}).get("description", skill_name)
    lines = [
        "---",
        f"name: {skill_name}",
        f"description: {description}",
        "version: 0.1.0",
        "allowed-tools:",
        "  - Bash",
        "  - Read",
        "  - Glob",
        "  - Grep",
    ]
    if source_hash:
        lines.append(f"source_hash: {source_hash}")
    lines.append("---")
    return "\n".join(lines)


def build_codex_frontmatter(skill_name: str, meta: dict, source_hash: str = "") -> str:
    """生成 Codex 格式的 YAML frontmatter。"""
    description = meta.get("codex", {}).get("description", skill_name)
    lines = ["---", f"name: {skill_name}", f"description: {description}"]
    if source_hash:
        lines.append(f"source_hash: {source_hash}")
    lines.append("---")
    return "\n".join(lines)


def build_openclaw_frontmatter(skill_name: str, meta: dict, source_hash: str = "") -> str:
    """生成 OpenClaw 格式的 YAML frontmatter。"""
    description = meta.get("codex", {}).get("description", skill_name)
    lines = ["---", f"name: {skill_name}", f"description: {description}"]
    if source_hash:
        lines.append(f"source_hash: {source_hash}")
    lines.append("---")
    return "\n".join(lines)


def inline_references(body: str, skill_name: str) -> str:
    """将 references/ 内容内联到正文末尾。"""
    refs = list_references(skill_name)
    if not refs:
        return body
    parts = [body, "", "## References (inlined)"]
    for ref in refs:
        parts.append(f"\n### {ref.name}")
        parts.append(ref.read_text().strip())
    return "\n".join(parts)


def sync_skill(skill_name: str, meta: dict, dry_run: bool = False):
    """同步单个 skill 到所有 agent。"""
    body = read_body(skill_name)
    refs = list_references(skill_name)
    scripts = list_scripts(skill_name)
    source_hash = compute_source_hash(skill_name, meta)

    print(f"\n{'[DRY RUN] ' if dry_run else ''}Syncing: {skill_name}")

    # --- Hermes ---
    category = meta.get("hermes", {}).get("category", "shared")
    hermes_path = AGENT_CONFIGS["hermes"]["base"] / category / skill_name / "SKILL.md"
    hermes_body = build_hermes_frontmatter(skill_name, meta, source_hash) + "\n\n" + inline_references(body, skill_name)
    if dry_run:
        print(f"  → Hermes: {hermes_path}")
    else:
        hermes_path.parent.mkdir(parents=True, exist_ok=True)
        hermes_path.write_text(hermes_body)
        print(f"  ✓ Hermes: {hermes_path}")

    # --- Claude Code ---
    claude_path = AGENT_CONFIGS["claude"]["base"] / skill_name / "SKILL.md"
    claude_body = build_claude_frontmatter(skill_name, meta, source_hash) + "\n\n" + body
    # Claude Code keeps references/ as separate files
    if dry_run:
        print(f"  → Claude: {claude_path}")
    else:
        claude_path.parent.mkdir(parents=True, exist_ok=True)
        claude_path.write_text(claude_body)
        # Copy references as separate files for Claude Code
        if refs:
            claude_refs_dir = claude_path.parent / "references"
            claude_refs_dir.mkdir(parents=True, exist_ok=True)
            for ref in refs:
                import shutil
                shutil.copy2(ref, claude_refs_dir / ref.name)
        print(f"  ✓ Claude: {claude_path} (+ {len(refs)} refs)")

    # --- Codex ---
    codex_path = AGENT_CONFIGS["codex"]["base"] / skill_name / "SKILL.md"
    codex_body = build_codex_frontmatter(skill_name, meta, source_hash) + "\n\n" + inline_references(body, skill_name)
    if dry_run:
        print(f"  → Codex: {codex_path}")
    else:
        codex_path.parent.mkdir(parents=True, exist_ok=True)
        codex_path.write_text(codex_body)
        print(f"  ✓ Codex: {codex_path}")

    # --- OpenClaw ---
    openclaw_path = AGENT_CONFIGS["openclaw"]["base"] / skill_name / "SKILL.md"
    openclaw_body = build_openclaw_frontmatter(skill_name, meta, source_hash) + "\n\n" + body
    if dry_run:
        print(f"  → OpenClaw: {openclaw_path}")
    else:
        openclaw_path.parent.mkdir(parents=True, exist_ok=True)
        openclaw_path.write_text(openclaw_body)
        # OpenClaw gets full references/ and scripts/ as separate files
        if refs:
            import shutil
            oc_refs_dir = openclaw_path.parent / "references"
            oc_refs_dir.mkdir(parents=True, exist_ok=True)
            for ref in refs:
                shutil.copy2(ref, oc_refs_dir / ref.name)
        if scripts:
            import shutil
            oc_scripts_dir = openclaw_path.parent / "scripts"
            oc_scripts_dir.mkdir(parents=True, exist_ok=True)
            for script in scripts:
                shutil.copy2(script, oc_scripts_dir / script.name)
        print(f"  ✓ OpenClaw: {openclaw_path} (+ {len(refs)} refs, + {len(scripts)} scripts)")


def get_agent_skill_path(agent: str, skill_name: str, meta: dict) -> Path | None:
    """Return the Path to an agent's copy of a skill, or None if the agent
    has no config for this skill type."""
    config = AGENT_CONFIGS.get(agent)
    if not config:
        return None
    base = config["base"]
    if agent == "hermes":
        category = meta.get("hermes", {}).get("category", "shared")
        return base / category / skill_name / "SKILL.md"
    return base / skill_name / "SKILL.md"


exec
/bin/bash -lc "sed -n '1,220p' observer/logs/developer_iter-1_20260705T180405Z.log" in /home/sean/shared-skills
 succeeded in 0ms:
=== COMMAND ===
claude -p --dangerously-skip-permissions Iteration 1 — Developer Operational Constraints:
- Read prd/PRD.md and prd/tech-design.md for requirements context
- Run tests after changes: python3 sync-skills.py --check 2>&1 | head -5
- Commit with: git add -A && git commit -m '<descriptive message>'
- Follow the Developer Instructions below for your specific task

# P2: Source Hash + --check Mode

## Goal
Track skill versions and detect when agent copies are outdated.

## Implementation
1. In sync-skills.py, before writing a skill to an agent directory:
   - Compute SHA256 of (SKILL.md + meta.yaml)
   - Write `source_hash: <sha256>` into the generated frontmatter
2. Add `--check` CLI argument:
   - Scan each agent's skill directory
   - Compare stored source_hash against current shared source
   - Report: "UP-TO-DATE", "OUTDATED", or "MISSING" for each skill-agent pair
3. Add `--verbose` flag to show hash values

Example output:
```
planning-with-files:
  hermes: OUTDATED (abc123 vs def456)
  claude: UP-TO-DATE
  codex: MISSING
```

Run test and commit.

## Phase Status
mode: code-dev, phase: dev, iteration: 1/3, prev_verdict: N/A, budget_remaining: 500000 tokens

=== OUTPUT ===
**Done.** Here's a summary of what was implemented:

### Changes to `sync-skills.py`

**New functions:**
- `compute_source_hash(skill_name, meta)` — computes SHA256 of raw `SKILL.md` bytes + serialized manifest metadata (with a `---META---` separator). Any change to either the skill body or its triggers/descriptions produces a different hash.
- `parse_frontmatter(filepath)` — extracts YAML frontmatter from agent `SKILL.md` files. Returns `None` for missing files, malformed YAML, or non-dict frontmatter.
- `get_agent_skill_path(agent, skill_name, meta)` — resolves the Path to an agent's copy of a skill (handles Hermes' category-based directory structure).
- `check_skills(verbose, targets)` — scans all agent copies, compares stored `source_hash` against current source, reports `UP-TO-DATE` / `OUTDATED (stored vs current)` / `MISSING`.

**Modified functions:**
- All four `build_*_frontmatter` functions now accept an optional `source_hash` parameter and inject it into the YAML frontmatter.
- `sync_skill` computes the hash once, passes it to each builder.
- `main` handles `--check` (runs `check_skills` and returns) and `--verbose` (shows hash values).

**Example output:**
```
$ python3 sync-skills.py --check | head -5
planning-with-files:
  hermes: UP-TO-DATE
  claude: UP-TO-DATE
  codex: UP-TO-DATE
  openclaw: UP-TO-DATE
```

exec
/bin/bash -lc 'find . -maxdepth 2 -name meta.yaml -print' in /home/sean/shared-skills
 succeeded in 0ms:
codex
A concrete issue is already visible: `build_openclaw_frontmatter()` still reads the Codex description. I’m checking whether that is in scope for this iteration’s requested P2 review or just inherited from another prompt before writing the verdict.
exec
/bin/bash -lc 'ls -la reviews' in /home/sean/shared-skills
 succeeded in 0ms:
total 12
drwxrwxr-x  2 sean sean 4096 Jul  6 02:04 .
drwxrwxr-x 14 sean sean 4096 Jul  6 02:07 ..
-rw-rw-r--  1 sean sean  482 Jul  6 02:04 acceptance-criteria.md

exec
/bin/bash -lc "sed -n '1,220p' skills-manifest.yaml" in /home/sean/shared-skills
 succeeded in 0ms:
# 共享 Skill 清单
# 每个 skill 定义其在各 agent 中的元数据（触发条件、描述、分类）
# 正文来自 ~/shared-skills/<name>/SKILL.md

planning-with-files:
  hermes:
    category: productivity
    triggers:
      - "plan with files"
      - "create a plan"
      - "task plan"
      - "制定计划"
      - "分步计划"
      - "multi step"
      - "crash proof"
  claude:
    description: "Crash-proof persistent file-based planning. Creates task_plan.md, findings.md, and progress.md on disk so any agent can survive context loss, crashes, and /clear."
  codex:
    description: "Crash-proof persistent file-based planning. Creates task_plan.md, findings.md, and progress.md on disk."

design-system:
  hermes:
    category: creative
    triggers:
      - "design system"
      - "DESIGN.md"
      - "UI design"
      - "设计系统"
      - "设计规范"
      - "visual identity"
      - "frontend style"
      - "CSS tokens"
      - "UI tokens"
      - "生成前端"
      - "build UI"
      - "design tokens"
  claude:
    description: "Global design system specification for all agent-generated UIs. Provides machine-readable design tokens and human-readable rules for HTML/CSS/JS generation."
  codex:
    description: "Global design system specification for agent-generated UIs. Provides design tokens (HSL colors, typography, spacing) and rules for frontend generation."

role-play:
  hermes:
    category: productivity
    triggers:
      - "role play"
      - "扮演"
      - "as a"
      - "act as"
      - "以.*身份"
      - "假装你是"
      - "你是.*角色"
      - "switch role"
  claude:
    description: "Assign a specific professional role before a task. Supports 8 roles: CEO, Designer, Eng Manager, QA, Release Manager, Doc Engineer, Security Auditor, Performance Engineer."
  codex:
    description: "Assign a specific professional role before a task. Supports 8 predefined roles."

handoff:
  hermes:
    category: devops
    triggers:
      - "handoff"
      - "交接"
      - "hand over"
      - "转交"
      - "给下一个 agent"
      - "把上下文给"
  claude:
    description: "Compress current conversation into a handoff document for another agent to continue. Answers: what was done, current status, next steps. Saves to ~/repos/_handoffs/."
  codex:
    description: "Compress current conversation into a handoff document for another agent."

codebase-design:
  hermes:
    category: software-development
    triggers:
      - "codebase design"
      - "架构评估"
      - "设计质量"
      - "模块边界"
      - "understand the architecture"
  claude:
    description: "Analyze codebase architecture: module boundaries, dependency direction, abstraction levels, SOLID principles. Score each dimension with file-path evidence."
  codex:
    description: "Analyze codebase architecture: modules, dependencies, abstractions."

domain-modeling:
  hermes:
    category: software-development
    triggers:
      - "domain model"
      - "领域模型"
      - "bounded context"
      - "聚合根"
      - "domain driven"
      - "业务术语"
      - "model the domain"
  claude:
    description: "Extract domain model from codebase: core entities, aggregate roots, value objects, domain events, bounded contexts. Produces a business model independent of tech implementation."
  codex:
    description: "Extract domain model from codebase: entities, value objects, bounded contexts."

diagnosing-bugs:
  hermes:
    category: software-development
    triggers:
      - "diagnosing bugs"
      - "diagnose"
      - "bug 诊断"
      - "排查"
      - "根因分析"
      - "root cause"
      - "debugging systematically"
  claude:
    description: "Systematic bug diagnosis: reproduce first, read full error trace, change one variable at a time, find root cause. Anti-patterns: shotgun debugging, workaround over root cause."
  codex:
    description: "Systematic bug diagnosis: reproduce, read error, one variable at a time, find root cause."

grill-design:
  hermes:
    category: software-development
    triggers:
      - "grill"
      - "grill me"
      - "审查这个设计"
      - "帮我打磨方案"
      - "这个方案有什么漏洞"
      - "检查设计"
  claude:
    description: "Deep design review: ask one question at a time along the design tree, provide recommended answers, document all decisions. End when all branches are resolved."
  codex:
    description: "Deep design review: one question at a time, document decisions."

exec
/bin/bash -lc "sed -n '260,620p' sync-skills.py" in /home/sean/shared-skills
 succeeded in 0ms:


def check_skills(verbose: bool = False, targets: list[str] | None = None):
    """Scan agent skill copies and compare stored source_hash against current source.

    For each skill in the manifest and each configured agent, reports:
    - UP-TO-DATE: stored hash matches current source
    - OUTDATED: stored hash differs from current source (agent copy is stale)
    - MISSING: no agent copy found, or no source_hash in its frontmatter

    If targets is provided, only check skills whose names are in the list.
    """
    manifest = load_manifest()

    # Agent display order
    agent_names = list(AGENT_CONFIGS.keys())

    for skill_name, meta in manifest.items():
        if targets and skill_name not in targets:
            continue
        current_hash = compute_source_hash(skill_name, meta)
        print(f"{skill_name}:")
        for agent in agent_names:
            path = get_agent_skill_path(agent, skill_name, meta)
            if path is None:
                continue
            fm = parse_frontmatter(path)
            if fm is None:
                print(f"  {agent}: MISSING")
                continue
            stored_hash = fm.get("source_hash")
            if not stored_hash:
                print(f"  {agent}: MISSING")
            elif stored_hash == current_hash:
                if verbose:
                    print(f"  {agent}: UP-TO-DATE ({current_hash[:12]})")
                else:
                    print(f"  {agent}: UP-TO-DATE")
            else:
                short_current = current_hash[:12]
                short_stored = stored_hash[:12] if len(stored_hash) >= 12 else stored_hash
                if verbose:
                    print(f"  {agent}: OUTDATED ({short_stored} vs {short_current})")
                else:
                    print(f"  {agent}: OUTDATED ({short_stored} vs {short_current})")


def main():
    dry_run = "--dry-run" in sys.argv
    check_mode = "--check" in sys.argv
    verbose = "--verbose" in sys.argv

    # Target specific skill(s) or all
    positional_args = [a for a in sys.argv[1:] if not a.startswith("--")]
    manifest = load_manifest()

    if positional_args:
        targets = [a for a in positional_args if a in manifest]
        if not targets:
            print(f"No skills found: {positional_args}")
            sys.exit(1)
    else:
        targets = list(manifest.keys())

    if check_mode:
        check_skills(verbose=verbose, targets=targets)
        return

    print(f"Sync source: {SHARED}")
    print(f"Targets: {', '.join(targets)}")
    if dry_run:
        print("Mode: DRY RUN (no files written)")

    for name in targets:
        sync_skill(name, manifest[name], dry_run=dry_run)

    print(f"\nDone. Synced {len(targets)} skill(s).")


if __name__ == "__main__":
    main()

exec
/bin/bash -lc 'git log --oneline -5 --decorate' in /home/sean/shared-skills
 succeeded in 0ms:
70b78f1 (HEAD -> master) feat: add source_hash tracking + --check mode to sync-skills.py
69e7494 init: shared-skills project

codex
The current committed change is the P2 implementation. I’m comparing it against `HEAD~1` so the review is about what changed in this iteration, not unrelated pipeline files or pre-existing code.
exec
/bin/bash -lc 'git diff HEAD~1..HEAD -- sync-skills.py' in /home/sean/shared-skills
 succeeded in 0ms:
diff --git a/sync-skills.py b/sync-skills.py
index 745ab50..548117d 100644
--- a/sync-skills.py
+++ b/sync-skills.py
@@ -5,6 +5,7 @@
 用法: python3 sync-skills.sh [--dry-run] [skill-name]
 """
 
+import hashlib
 import os
 import sys
 import yaml
@@ -63,7 +64,46 @@ def list_scripts(skill_name: str) -> list[Path]:
     return []
 
 
-def build_hermes_frontmatter(skill_name: str, meta: dict) -> str:
+def compute_source_hash(skill_name: str, meta: dict) -> str:
+    """Compute SHA256 hash of raw SKILL.md + serialized manifest metadata.
+
+    This hash captures the full shared source for a skill — both its body
+    and its agent-specific metadata (triggers, categories, descriptions).
+    Any change to either will produce a different hash, which --check uses
+    to detect outdated agent copies.
+    """
+    skill_path = SHARED / skill_name / "SKILL.md"
+    skill_bytes = skill_path.read_bytes()
+    meta_bytes = yaml.dump(meta, sort_keys=True, allow_unicode=True).encode("utf-8")
+    combined = skill_bytes + b"\n---META---\n" + meta_bytes
+    return hashlib.sha256(combined).hexdigest()
+
+
+def parse_frontmatter(filepath: Path) -> dict | None:
+    """Parse YAML frontmatter from a SKILL.md file.
+
+    Returns the parsed dict on success, None if the file is missing,
+    has no frontmatter, or the YAML is invalid.
+    """
+    if not filepath.exists():
+        return None
+    content = filepath.read_text()
+    if not content.startswith("---"):
+        return None
+    end = content.find("---", 3)
+    if end == -1:
+        return None
+    fm_text = content[3:end].strip()
+    if not fm_text:
+        return None
+    try:
+        parsed = yaml.safe_load(fm_text)
+        return parsed if isinstance(parsed, dict) else None
+    except yaml.YAMLError:
+        return None
+
+
+def build_hermes_frontmatter(skill_name: str, meta: dict, source_hash: str = "") -> str:
     """生成 Hermes 格式的 YAML frontmatter（含 triggers）。"""
     triggers = meta.get("hermes", {}).get("triggers", [])
     description = meta.get("claude", {}).get("description", skill_name)
@@ -76,11 +116,13 @@ def build_hermes_frontmatter(skill_name: str, meta: dict) -> str:
                 lines.append(f'  - "{t}"')
             else:
                 lines.append(f"  - {t}")
+    if source_hash:
+        lines.append(f"source_hash: {source_hash}")
     lines.append("---")
     return "\n".join(lines)
 
 
-def build_claude_frontmatter(skill_name: str, meta: dict) -> str:
+def build_claude_frontmatter(skill_name: str, meta: dict, source_hash: str = "") -> str:
     """生成 Claude Code 格式的 YAML frontmatter。"""
     description = meta.get("claude", {}).get("description", skill_name)
     lines = [
@@ -93,21 +135,31 @@ def build_claude_frontmatter(skill_name: str, meta: dict) -> str:
         "  - Read",
         "  - Glob",
         "  - Grep",
-        "---",
     ]
+    if source_hash:
+        lines.append(f"source_hash: {source_hash}")
+    lines.append("---")
     return "\n".join(lines)
 
 
-def build_codex_frontmatter(skill_name: str, meta: dict) -> str:
+def build_codex_frontmatter(skill_name: str, meta: dict, source_hash: str = "") -> str:
     """生成 Codex 格式的 YAML frontmatter。"""
     description = meta.get("codex", {}).get("description", skill_name)
-    return f"---\nname: {skill_name}\ndescription: {description}\n---"
+    lines = ["---", f"name: {skill_name}", f"description: {description}"]
+    if source_hash:
+        lines.append(f"source_hash: {source_hash}")
+    lines.append("---")
+    return "\n".join(lines)
 
 
-def build_openclaw_frontmatter(skill_name: str, meta: dict) -> str:
+def build_openclaw_frontmatter(skill_name: str, meta: dict, source_hash: str = "") -> str:
     """生成 OpenClaw 格式的 YAML frontmatter。"""
     description = meta.get("codex", {}).get("description", skill_name)
-    return f"---\nname: {skill_name}\ndescription: {description}\n---"
+    lines = ["---", f"name: {skill_name}", f"description: {description}"]
+    if source_hash:
+        lines.append(f"source_hash: {source_hash}")
+    lines.append("---")
+    return "\n".join(lines)
 
 
 def inline_references(body: str, skill_name: str) -> str:
@@ -127,13 +179,14 @@ def sync_skill(skill_name: str, meta: dict, dry_run: bool = False):
     body = read_body(skill_name)
     refs = list_references(skill_name)
     scripts = list_scripts(skill_name)
+    source_hash = compute_source_hash(skill_name, meta)
 
     print(f"\n{'[DRY RUN] ' if dry_run else ''}Syncing: {skill_name}")
 
     # --- Hermes ---
     category = meta.get("hermes", {}).get("category", "shared")
     hermes_path = AGENT_CONFIGS["hermes"]["base"] / category / skill_name / "SKILL.md"
-    hermes_body = build_hermes_frontmatter(skill_name, meta) + "\n\n" + inline_references(body, skill_name)
+    hermes_body = build_hermes_frontmatter(skill_name, meta, source_hash) + "\n\n" + inline_references(body, skill_name)
     if dry_run:
         print(f"  → Hermes: {hermes_path}")
     else:
@@ -143,7 +196,7 @@ def sync_skill(skill_name: str, meta: dict, dry_run: bool = False):
 
     # --- Claude Code ---
     claude_path = AGENT_CONFIGS["claude"]["base"] / skill_name / "SKILL.md"
-    claude_body = build_claude_frontmatter(skill_name, meta) + "\n\n" + body
+    claude_body = build_claude_frontmatter(skill_name, meta, source_hash) + "\n\n" + body
     # Claude Code keeps references/ as separate files
     if dry_run:
         print(f"  → Claude: {claude_path}")
@@ -161,7 +214,7 @@ def sync_skill(skill_name: str, meta: dict, dry_run: bool = False):
 
     # --- Codex ---
     codex_path = AGENT_CONFIGS["codex"]["base"] / skill_name / "SKILL.md"
-    codex_body = build_codex_frontmatter(skill_name, meta) + "\n\n" + inline_references(body, skill_name)
+    codex_body = build_codex_frontmatter(skill_name, meta, source_hash) + "\n\n" + inline_references(body, skill_name)
     if dry_run:
         print(f"  → Codex: {codex_path}")
     else:
@@ -171,7 +224,7 @@ def sync_skill(skill_name: str, meta: dict, dry_run: bool = False):
 
     # --- OpenClaw ---
     openclaw_path = AGENT_CONFIGS["openclaw"]["base"] / skill_name / "SKILL.md"
-    openclaw_body = build_openclaw_frontmatter(skill_name, meta) + "\n\n" + body
+    openclaw_body = build_openclaw_frontmatter(skill_name, meta, source_hash) + "\n\n" + body
     if dry_run:
         print(f"  → OpenClaw: {openclaw_path}")
     else:
@@ -193,21 +246,85 @@ def sync_skill(skill_name: str, meta: dict, dry_run: bool = False):
         print(f"  ✓ OpenClaw: {openclaw_path} (+ {len(refs)} refs, + {len(scripts)} scripts)")
 
 
+def get_agent_skill_path(agent: str, skill_name: str, meta: dict) -> Path | None:
+    """Return the Path to an agent's copy of a skill, or None if the agent
+    has no config for this skill type."""
+    config = AGENT_CONFIGS.get(agent)
+    if not config:
+        return None
+    base = config["base"]
+    if agent == "hermes":
+        category = meta.get("hermes", {}).get("category", "shared")
+        return base / category / skill_name / "SKILL.md"
+    return base / skill_name / "SKILL.md"
+
+
+def check_skills(verbose: bool = False, targets: list[str] | None = None):
+    """Scan agent skill copies and compare stored source_hash against current source.
+
+    For each skill in the manifest and each configured agent, reports:
+    - UP-TO-DATE: stored hash matches current source
+    - OUTDATED: stored hash differs from current source (agent copy is stale)
+    - MISSING: no agent copy found, or no source_hash in its frontmatter
+
+    If targets is provided, only check skills whose names are in the list.
+    """
+    manifest = load_manifest()
+
+    # Agent display order
+    agent_names = list(AGENT_CONFIGS.keys())
+
+    for skill_name, meta in manifest.items():
+        if targets and skill_name not in targets:
+            continue
+        current_hash = compute_source_hash(skill_name, meta)
+        print(f"{skill_name}:")
+        for agent in agent_names:
+            path = get_agent_skill_path(agent, skill_name, meta)
+            if path is None:
+                continue
+            fm = parse_frontmatter(path)
+            if fm is None:
+                print(f"  {agent}: MISSING")
+                continue
+            stored_hash = fm.get("source_hash")
+            if not stored_hash:
+                print(f"  {agent}: MISSING")
+            elif stored_hash == current_hash:
+                if verbose:
+                    print(f"  {agent}: UP-TO-DATE ({current_hash[:12]})")
+                else:
+                    print(f"  {agent}: UP-TO-DATE")
+            else:
+                short_current = current_hash[:12]
+                short_stored = stored_hash[:12] if len(stored_hash) >= 12 else stored_hash
+                if verbose:
+                    print(f"  {agent}: OUTDATED ({short_stored} vs {short_current})")
+                else:
+                    print(f"  {agent}: OUTDATED ({short_stored} vs {short_current})")
+
+
 def main():
     dry_run = "--dry-run" in sys.argv
+    check_mode = "--check" in sys.argv
+    verbose = "--verbose" in sys.argv
 
     # Target specific skill(s) or all
-    args = [a for a in sys.argv[1:] if not a.startswith("--")]
+    positional_args = [a for a in sys.argv[1:] if not a.startswith("--")]
     manifest = load_manifest()
 
-    if args:
-        targets = [a for a in args if a in manifest]
+    if positional_args:
+        targets = [a for a in positional_args if a in manifest]
         if not targets:
-            print(f"No skills found: {args}")
+            print(f"No skills found: {positional_args}")
             sys.exit(1)
     else:
         targets = list(manifest.keys())
 
+    if check_mode:
+        check_skills(verbose=verbose, targets=targets)
+        return
+
     print(f"Sync source: {SHARED}")
     print(f"Targets: {', '.join(targets)}")
     if dry_run:

exec
/bin/bash -lc 'git show --stat --oneline HEAD' in /home/sean/shared-skills
 succeeded in 0ms:
70b78f1 feat: add source_hash tracking + --check mode to sync-skills.py
 .unison/pipelines/p0.yaml                          |  13 ++
 .unison/pipelines/p1.yaml                          |  13 ++
 .unison/pipelines/p2.yaml                          |  13 ++
 .unison/pipelines/p3.yaml                          |  13 ++
 .unison/prompts/dev-p0.md                          |  13 ++
 .unison/prompts/dev-p1.md                          |  37 ++++++
 .unison/prompts/dev-p2.md                          |  24 ++++
 .unison/prompts/dev-p3.md                          |  15 +++
 .unison/prompts/review-generic.md                  |  11 ++
 .unison/run.log                                    | 113 ++++++++++++++++
 .../logs/developer_iter-1_20260705T180405Z.log     |  36 +++++
 reviews/acceptance-criteria.md                     |  17 +++
 sync-skills.py                                     | 147 ++++++++++++++++++---
 13 files changed, 450 insertions(+), 15 deletions(-)

exec
/bin/bash -lc "git show HEAD~1:sync-skills.py | sed -n '1,260p'" in /home/sean/shared-skills
 succeeded in 0ms:
#!/usr/bin/env python3
"""sync-skills — 将 ~/shared-skills/ 同步到各 agent 的 skill 目录。

单向同步：共享源 → agent。修改必须从共享源开始。
用法: python3 sync-skills.sh [--dry-run] [skill-name]
"""

import os
import sys
import yaml
from pathlib import Path

SHARED = Path.home() / "shared-skills"
MANIFEST = SHARED / "skills-manifest.yaml"

AGENT_CONFIGS = {
    "hermes": {
        "base": Path.home() / ".hermes" / "skills",
        "frontmatter_fields": ["name", "description", "triggers"],
    },
    "claude": {
        "base": Path.home() / ".claude" / "skills",
        "frontmatter_fields": ["name", "description", "version", "allowed-tools"],
        "extra": {"version": "0.1.0", "allowed-tools": ["Bash", "Read", "Glob", "Grep"]},
    },
    "codex": {
        "base": Path.home() / ".codex" / "skills",
        "frontmatter_fields": ["name", "description"],
    },
    "openclaw": {
        "base": Path.home() / ".openclaw" / "agents" / "main" / "skills",
        "frontmatter_fields": ["name", "description"],
    },
}


def load_manifest():
    with open(MANIFEST) as f:
        return yaml.safe_load(f)


def read_body(skill_name: str) -> str:
    """读取共享 skill 的正文（去除空行首尾）。"""
    path = SHARED / skill_name / "SKILL.md"
    if not path.exists():
        raise FileNotFoundError(f"Missing shared SKILL.md: {path}")
    return path.read_text().strip()


def list_references(skill_name: str) -> list[Path]:
    """列出 references/ 下的文件。"""
    ref_dir = SHARED / skill_name / "references"
    if ref_dir.exists():
        return sorted(ref_dir.glob("*.md"))
    return []


def list_scripts(skill_name: str) -> list[Path]:
    """列出 scripts/ 下的文件。"""
    scripts_dir = SHARED / skill_name / "scripts"
    if scripts_dir.exists():
        return sorted(scripts_dir.glob("*"))
    return []


def build_hermes_frontmatter(skill_name: str, meta: dict) -> str:
    """生成 Hermes 格式的 YAML frontmatter（含 triggers）。"""
    triggers = meta.get("hermes", {}).get("triggers", [])
    description = meta.get("claude", {}).get("description", skill_name)
    lines = ["---", f"name: {skill_name}", f"description: {description}"]
    if triggers:
        lines.append("triggers:")
        for t in triggers:
            # regex patterns with special chars need quoting
            if any(c in t for c in ".*{}[]()"):
                lines.append(f'  - "{t}"')
            else:
                lines.append(f"  - {t}")
    lines.append("---")
    return "\n".join(lines)


def build_claude_frontmatter(skill_name: str, meta: dict) -> str:
    """生成 Claude Code 格式的 YAML frontmatter。"""
    description = meta.get("claude", {}).get("description", skill_name)
    lines = [
        "---",
        f"name: {skill_name}",
        f"description: {description}",
        "version: 0.1.0",
        "allowed-tools:",
        "  - Bash",
        "  - Read",
        "  - Glob",
        "  - Grep",
        "---",
    ]
    return "\n".join(lines)


def build_codex_frontmatter(skill_name: str, meta: dict) -> str:
    """生成 Codex 格式的 YAML frontmatter。"""
    description = meta.get("codex", {}).get("description", skill_name)
    return f"---\nname: {skill_name}\ndescription: {description}\n---"


def build_openclaw_frontmatter(skill_name: str, meta: dict) -> str:
    """生成 OpenClaw 格式的 YAML frontmatter。"""
    description = meta.get("codex", {}).get("description", skill_name)
    return f"---\nname: {skill_name}\ndescription: {description}\n---"


def inline_references(body: str, skill_name: str) -> str:
    """将 references/ 内容内联到正文末尾。"""
    refs = list_references(skill_name)
    if not refs:
        return body
    parts = [body, "", "## References (inlined)"]
    for ref in refs:
        parts.append(f"\n### {ref.name}")
        parts.append(ref.read_text().strip())
    return "\n".join(parts)


def sync_skill(skill_name: str, meta: dict, dry_run: bool = False):
    """同步单个 skill 到所有 agent。"""
    body = read_body(skill_name)
    refs = list_references(skill_name)
    scripts = list_scripts(skill_name)

    print(f"\n{'[DRY RUN] ' if dry_run else ''}Syncing: {skill_name}")

    # --- Hermes ---
    category = meta.get("hermes", {}).get("category", "shared")
    hermes_path = AGENT_CONFIGS["hermes"]["base"] / category / skill_name / "SKILL.md"
    hermes_body = build_hermes_frontmatter(skill_name, meta) + "\n\n" + inline_references(body, skill_name)
    if dry_run:
        print(f"  → Hermes: {hermes_path}")
    else:
        hermes_path.parent.mkdir(parents=True, exist_ok=True)
        hermes_path.write_text(hermes_body)
        print(f"  ✓ Hermes: {hermes_path}")

    # --- Claude Code ---
    claude_path = AGENT_CONFIGS["claude"]["base"] / skill_name / "SKILL.md"
    claude_body = build_claude_frontmatter(skill_name, meta) + "\n\n" + body
    # Claude Code keeps references/ as separate files
    if dry_run:
        print(f"  → Claude: {claude_path}")
    else:
        claude_path.parent.mkdir(parents=True, exist_ok=True)
        claude_path.write_text(claude_body)
        # Copy references as separate files for Claude Code
        if refs:
            claude_refs_dir = claude_path.parent / "references"
            claude_refs_dir.mkdir(parents=True, exist_ok=True)
            for ref in refs:
                import shutil
                shutil.copy2(ref, claude_refs_dir / ref.name)
        print(f"  ✓ Claude: {claude_path} (+ {len(refs)} refs)")

    # --- Codex ---
    codex_path = AGENT_CONFIGS["codex"]["base"] / skill_name / "SKILL.md"
    codex_body = build_codex_frontmatter(skill_name, meta) + "\n\n" + inline_references(body, skill_name)
    if dry_run:
        print(f"  → Codex: {codex_path}")
    else:
        codex_path.parent.mkdir(parents=True, exist_ok=True)
        codex_path.write_text(codex_body)
        print(f"  ✓ Codex: {codex_path}")

    # --- OpenClaw ---
    openclaw_path = AGENT_CONFIGS["openclaw"]["base"] / skill_name / "SKILL.md"
    openclaw_body = build_openclaw_frontmatter(skill_name, meta) + "\n\n" + body
    if dry_run:
        print(f"  → OpenClaw: {openclaw_path}")
    else:
        openclaw_path.parent.mkdir(parents=True, exist_ok=True)
        openclaw_path.write_text(openclaw_body)
        # OpenClaw gets full references/ and scripts/ as separate files
        if refs:
            import shutil
            oc_refs_dir = openclaw_path.parent / "references"
            oc_refs_dir.mkdir(parents=True, exist_ok=True)
            for ref in refs:
                shutil.copy2(ref, oc_refs_dir / ref.name)
        if scripts:
            import shutil
            oc_scripts_dir = openclaw_path.parent / "scripts"
            oc_scripts_dir.mkdir(parents=True, exist_ok=True)
            for script in scripts:
                shutil.copy2(script, oc_scripts_dir / script.name)
        print(f"  ✓ OpenClaw: {openclaw_path} (+ {len(refs)} refs, + {len(scripts)} scripts)")


def main():
    dry_run = "--dry-run" in sys.argv

    # Target specific skill(s) or all
    args = [a for a in sys.argv[1:] if not a.startswith("--")]
    manifest = load_manifest()

    if args:
        targets = [a for a in args if a in manifest]
        if not targets:
            print(f"No skills found: {args}")
            sys.exit(1)
    else:
        targets = list(manifest.keys())

    print(f"Sync source: {SHARED}")
    print(f"Targets: {', '.join(targets)}")
    if dry_run:
        print("Mode: DRY RUN (no files written)")

    for name in targets:
        sync_skill(name, manifest[name], dry_run=dry_run)

    print(f"\nDone. Synced {len(targets)} skill(s).")


if __name__ == "__main__":
    main()

codex
There are also newly committed `.unison/pipelines/*.yaml` files in this iteration. I’m validating those because they are part of the developer output shown in the diff, even though the requested smoke test does not exercise them.
exec
/bin/bash -lc 'nl -ba .unison/pipelines/p0.yaml && nl -ba .unison/pipelines/p1.yaml && nl -ba .unison/pipelines/p3.yaml' in /home/sean/shared-skills
 succeeded in 0ms:
     1	version: "2.0"
     2	project_root: "/home/sean/shared-skills"
     3	mode: "code-dev"
     4	agents:
     5	  developer: {role: developer, runtime: claude, model: claude-sonnet-4-6, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/dev-p0.md", pipeline_role: developer}
     6	  reviewer:  {role: reviewer,  runtime: codex, model: gpt-5.5, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/review-generic.md", pipeline_role: reviewer}
     7	project:
     8	  test_command: "python3 -c "import yaml; m=yaml.safe_load(open('skills-manifest.yaml')); assert all(k in m for k in ['planning-with-files','design-system']); print('OK')""
     9	  max_iterations: 3
    10	  per_agent_timeout: 600
    11	budget:
    12	  daily_token_limit: 5000000
    13	  per_task_limit: 500000
     1	version: "2.0"
     2	project_root: "/home/sean/shared-skills"
     3	mode: "code-dev"
     4	agents:
     5	  developer: {role: developer, runtime: claude, model: claude-sonnet-4-6, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/dev-p1.md", pipeline_role: developer}
     6	  reviewer:  {role: reviewer,  runtime: codex, model: gpt-5.5, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/review-generic.md", pipeline_role: reviewer}
     7	project:
     8	  test_command: "python3 -c "import os,yaml; [print(f'{d}: OK') for d in os.listdir('.') if os.path.isdir(d) and not d.startswith('.') and os.path.exists(f'{d}/meta.yaml')]""
     9	  max_iterations: 3
    10	  per_agent_timeout: 600
    11	budget:
    12	  daily_token_limit: 5000000
    13	  per_task_limit: 500000
     1	version: "2.0"
     2	project_root: "/home/sean/shared-skills"
     3	mode: "code-dev"
     4	agents:
     5	  developer: {role: developer, runtime: claude, model: claude-sonnet-4-6, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/dev-p3.md", pipeline_role: developer}
     6	  reviewer:  {role: reviewer,  runtime: codex, model: gpt-5.5, system_prompt_path: "/home/sean/shared-skills/.unison/prompts/review-generic.md", pipeline_role: reviewer}
     7	project:
     8	  test_command: "python3 -c "import json; [print(json.loads(l)['skill']) for l in open('sync.log') if l.strip()]" 2>/dev/null || python3 -c "print('sync log will be created on first sync')""
     9	  max_iterations: 3
    10	  per_agent_timeout: 600
    11	budget:
    12	  daily_token_limit: 5000000
    13	  per_task_limit: 500000

exec
/bin/bash -lc "nl -ba sync-skills.py | sed -n '64,170p'" in /home/sean/shared-skills
 succeeded in 0ms:
    64	    return []
    65	
    66	
    67	def compute_source_hash(skill_name: str, meta: dict) -> str:
    68	    """Compute SHA256 hash of raw SKILL.md + serialized manifest metadata.
    69	
    70	    This hash captures the full shared source for a skill — both its body
    71	    and its agent-specific metadata (triggers, categories, descriptions).
    72	    Any change to either will produce a different hash, which --check uses
    73	    to detect outdated agent copies.
    74	    """
    75	    skill_path = SHARED / skill_name / "SKILL.md"
    76	    skill_bytes = skill_path.read_bytes()
    77	    meta_bytes = yaml.dump(meta, sort_keys=True, allow_unicode=True).encode("utf-8")
    78	    combined = skill_bytes + b"\n---META---\n" + meta_bytes
    79	    return hashlib.sha256(combined).hexdigest()
    80	
    81	
    82	def parse_frontmatter(filepath: Path) -> dict | None:
    83	    """Parse YAML frontmatter from a SKILL.md file.
    84	
    85	    Returns the parsed dict on success, None if the file is missing,
    86	    has no frontmatter, or the YAML is invalid.
    87	    """
    88	    if not filepath.exists():
    89	        return None
    90	    content = filepath.read_text()
    91	    if not content.startswith("---"):
    92	        return None
    93	    end = content.find("---", 3)
    94	    if end == -1:
    95	        return None
    96	    fm_text = content[3:end].strip()
    97	    if not fm_text:
    98	        return None
    99	    try:
   100	        parsed = yaml.safe_load(fm_text)
   101	        return parsed if isinstance(parsed, dict) else None
   102	    except yaml.YAMLError:
   103	        return None
   104	
   105	
   106	def build_hermes_frontmatter(skill_name: str, meta: dict, source_hash: str = "") -> str:
   107	    """生成 Hermes 格式的 YAML frontmatter（含 triggers）。"""
   108	    triggers = meta.get("hermes", {}).get("triggers", [])
   109	    description = meta.get("claude", {}).get("description", skill_name)
   110	    lines = ["---", f"name: {skill_name}", f"description: {description}"]
   111	    if triggers:
   112	        lines.append("triggers:")
   113	        for t in triggers:
   114	            # regex patterns with special chars need quoting
   115	            if any(c in t for c in ".*{}[]()"):
   116	                lines.append(f'  - "{t}"')
   117	            else:
   118	                lines.append(f"  - {t}")
   119	    if source_hash:
   120	        lines.append(f"source_hash: {source_hash}")
   121	    lines.append("---")
   122	    return "\n".join(lines)
   123	
   124	
   125	def build_claude_frontmatter(skill_name: str, meta: dict, source_hash: str = "") -> str:
   126	    """生成 Claude Code 格式的 YAML frontmatter。"""
   127	    description = meta.get("claude", {}).get("description", skill_name)
   128	    lines = [
   129	        "---",
   130	        f"name: {skill_name}",
   131	        f"description: {description}",
   132	        "version: 0.1.0",
   133	        "allowed-tools:",
   134	        "  - Bash",
   135	        "  - Read",
   136	        "  - Glob",
   137	        "  - Grep",
   138	    ]
   139	    if source_hash:
   140	        lines.append(f"source_hash: {source_hash}")
   141	    lines.append("---")
   142	    return "\n".join(lines)
   143	
   144	
   145	def build_codex_frontmatter(skill_name: str, meta: dict, source_hash: str = "") -> str:
   146	    """生成 Codex 格式的 YAML frontmatter。"""
   147	    description = meta.get("codex", {}).get("description", skill_name)
   148	    lines = ["---", f"name: {skill_name}", f"description: {description}"]
   149	    if source_hash:
   150	        lines.append(f"source_hash: {source_hash}")
   151	    lines.append("---")
   152	    return "\n".join(lines)
   153	
   154	
   155	def build_openclaw_frontmatter(skill_name: str, meta: dict, source_hash: str = "") -> str:
   156	    """生成 OpenClaw 格式的 YAML frontmatter。"""
   157	    description = meta.get("codex", {}).get("description", skill_name)
   158	    lines = ["---", f"name: {skill_name}", f"description: {description}"]
   159	    if source_hash:
   160	        lines.append(f"source_hash: {source_hash}")
   161	    lines.append("---")
   162	    return "\n".join(lines)
   163	
   164	
   165	def inline_references(body: str, skill_name: str) -> str:
   166	    """将 references/ 内容内联到正文末尾。"""
   167	    refs = list_references(skill_name)
   168	    if not refs:
   169	        return body
   170	    parts = [body, "", "## References (inlined)"]

exec
/bin/bash -lc "python3 - <<'PY'
import yaml
from pathlib import Path
for p in sorted(Path('.unison/pipelines').glob('*.yaml')):
    try:
        yaml.safe_load(p.read_text())
        print(f'{p}: OK')
    except Exception as e:
        print(f'{p}: ERROR: {e.__class__.__name__}: {e}')
PY" in /home/sean/shared-skills
 succeeded in 0ms:
.unison/pipelines/p0.yaml: ERROR: ParserError: while parsing a block mapping
  in "<unicode string>", line 8, column 3:
      test_command: "python3 -c "impor ... 
      ^
expected <block end>, but found '<scalar>'
  in "<unicode string>", line 8, column 30:
      test_command: "python3 -c "import yaml; m=yaml.safe_load(op ... 
                                 ^
.unison/pipelines/p1.yaml: ERROR: ParserError: while parsing a block mapping
  in "<unicode string>", line 8, column 3:
      test_command: "python3 -c "impor ... 
      ^
expected <block end>, but found '<scalar>'
  in "<unicode string>", line 8, column 30:
      test_command: "python3 -c "import os,yaml; [print(f'{d}: OK ... 
                                 ^
.unison/pipelines/p2.yaml: OK
.unison/pipelines/p3.yaml: ERROR: ParserError: while parsing a block mapping
  in "<unicode string>", line 8, column 3:
      test_command: "python3 -c "impor ... 
      ^
expected <block end>, but found '<scalar>'
  in "<unicode string>", line 8, column 30:
      test_command: "python3 -c "import json; [print(json.loads(l ... 
                                 ^

codex
The requested smoke check passes, but I found invalid YAML in three newly added pipeline configs. I’m writing the iteration review with a fail verdict because those files are part of the committed output and will break pipeline loading.
exec
/bin/bash -lc "sed -n '1,220p' reviews/acceptance-criteria.md" in /home/sean/shared-skills
 succeeded in 0ms:
# Acceptance Criteria (FROZEN)

**Frozen at:** 2026-07-05T18:04:05Z
**Test command:** `python3 sync-skills.py --check 2>&1 | head -5`
**Max iterations:** 3

## Rules for Reviewer

1. Judge against these criteria — do not add new criteria mid-review
2. If the code passes the test command, it meets minimum bar
3. REQUEST_CHANGES only for: test failures, security issues, architectural violations

## Project Test Command

```bash
python3 sync-skills.py --check 2>&1 | head -5
```

exec
/bin/bash -lc 'python3 sync-skills.py --check --verbose 2>&1 | head -10' in /home/sean/shared-skills
 succeeded in 470ms:
planning-with-files:
  hermes: UP-TO-DATE (1a4132de4055)
  claude: UP-TO-DATE (1a4132de4055)
  codex: UP-TO-DATE (1a4132de4055)
  openclaw: UP-TO-DATE (1a4132de4055)
design-system:
  hermes: MISSING
  claude: MISSING
  codex: MISSING
  openclaw: MISSING

codex
I’m adding `reviews/iter-1.md` now. It will record the passing requested test and the blocking pipeline YAML parse issue with file/line evidence.
apply patch
patch: completed
/home/sean/shared-skills/reviews/iter-1.md
diff --git a/reviews/iter-1.md b/reviews/iter-1.md
new file mode 100644
index 0000000000000000000000000000000000000000..8a4b163bed97fd2a3ed0da7d56bd34a5fe1243d2
--- /dev/null
+++ b/reviews/iter-1.md
@@ -0,0 +1,23 @@
+---
+verdict: REQUEST_CHANGES
+summary: "Requested check passes, but committed pipeline YAML files are unparsable."
+findings:
+  - severity: blocker
+    category: correctness
+    summary: ".unison/pipelines/p0.yaml, p1.yaml, and p3.yaml have unescaped nested double quotes in project.test_command, so PyYAML cannot parse those pipeline definitions."
+    evidence:
+      - ".unison/pipelines/p0.yaml:8"
+      - ".unison/pipelines/p1.yaml:8"
+      - ".unison/pipelines/p3.yaml:8"
+metrics:
+  test_command: "python3 sync-skills.py --check 2>&1 | head -5"
+  test_exit_code: 0
+  test_output:
+    - "planning-with-files:"
+    - "  hermes: UP-TO-DATE"
+    - "  claude: UP-TO-DATE"
+    - "  codex: UP-TO-DATE"
+    - "  openclaw: UP-TO-DATE"
+  additional_checks:
+    pipeline_yaml_parse: "3 failed, 1 passed"
+---

diff --git a/reviews/iter-1.md b/reviews/iter-1.md
new file mode 100644
index 0000000000000000000000000000000000000000..8a4b163bed97fd2a3ed0da7d56bd34a5fe1243d2
--- /dev/null
+++ b/reviews/iter-1.md
@@ -0,0 +1,23 @@
+---
+verdict: REQUEST_CHANGES
+summary: "Requested check passes, but committed pipeline YAML files are unparsable."
+findings:
+  - severity: blocker
+    category: correctness
+    summary: ".unison/pipelines/p0.yaml, p1.yaml, and p3.yaml have unescaped nested double quotes in project.test_command, so PyYAML cannot parse those pipeline definitions."
+    evidence:
+      - ".unison/pipelines/p0.yaml:8"
+      - ".unison/pipelines/p1.yaml:8"
+      - ".unison/pipelines/p3.yaml:8"
+metrics:
+  test_command: "python3 sync-skills.py --check 2>&1 | head -5"
+  test_exit_code: 0
+  test_output:
+    - "planning-with-files:"
+    - "  hermes: UP-TO-DATE"
+    - "  claude: UP-TO-DATE"
+    - "  codex: UP-TO-DATE"
+    - "  openclaw: UP-TO-DATE"
+  additional_checks:
+    pipeline_yaml_parse: "3 failed, 1 passed"
+---

codex
The review file is written. I’m doing one quick validation that its frontmatter parses cleanly and that only the review artifact was added by me.
exec
/bin/bash -lc 'git status --short' in /home/sean/shared-skills
 succeeded in 0ms:
 M observer/logs/developer_iter-1_20260705T180405Z.log
?? .unison/budget.json
?? observer/logs/reviewer_iter-1_20260705T180742Z.log
?? reviews/iter-1.md

exec
/bin/bash -lc "python3 - <<'PY'
import yaml
from pathlib import Path
p=Path('reviews/iter-1.md')
text=p.read_text()
end=text.find('---',3)
print(yaml.safe_load(text[3:end]))
PY" in /home/sean/shared-skills
 succeeded in 0ms:
{'verdict': 'REQUEST_CHANGES', 'summary': 'Requested check passes, but committed pipeline YAML files are unparsable.', 'findings': [{'severity': 'blocker', 'category': 'correctness', 'summary': '.unison/pipelines/p0.yaml, p1.yaml, and p3.yaml have unescaped nested double quotes in project.test_command, so PyYAML cannot parse those pipeline definitions.', 'evidence': ['.unison/pipelines/p0.yaml:8', '.unison/pipelines/p1.yaml:8', '.unison/pipelines/p3.yaml:8']}], 'metrics': {'test_command': 'python3 sync-skills.py --check 2>&1 | head -5', 'test_exit_code': 0, 'test_output': ['planning-with-files:', '  hermes: UP-TO-DATE', '  claude: UP-TO-DATE', '  codex: UP-TO-DATE', '  openclaw: UP-TO-DATE'], 'additional_checks': {'pipeline_yaml_parse': '3 failed, 1 passed'}}}

diff --git a/reviews/iter-1.md b/reviews/iter-1.md
new file mode 100644
index 0000000000000000000000000000000000000000..8a4b163bed97fd2a3ed0da7d56bd34a5fe1243d2
--- /dev/null
+++ b/reviews/iter-1.md
@@ -0,0 +1,23 @@
+---
+verdict: REQUEST_CHANGES
+summary: "Requested check passes, but committed pipeline YAML files are unparsable."
+findings:
+  - severity: blocker
+    category: correctness
+    summary: ".unison/pipelines/p0.yaml, p1.yaml, and p3.yaml have unescaped nested double quotes in project.test_command, so PyYAML cannot parse those pipeline definitions."
+    evidence:
+      - ".unison/pipelines/p0.yaml:8"
+      - ".unison/pipelines/p1.yaml:8"
+      - ".unison/pipelines/p3.yaml:8"
+metrics:
+  test_command: "python3 sync-skills.py --check 2>&1 | head -5"
+  test_exit_code: 0
+  test_output:
+    - "planning-with-files:"
+    - "  hermes: UP-TO-DATE"
+    - "  claude: UP-TO-DATE"
+    - "  codex: UP-TO-DATE"
+    - "  openclaw: UP-TO-DATE"
+  additional_checks:
+    pipeline_yaml_parse: "3 failed, 1 passed"
+---

codex
Wrote the review to [reviews/iter-1.md](/home/sean/shared-skills/reviews/iter-1.md).

Verdict: `REQUEST_CHANGES`

Requested test passed:

```bash
python3 sync-skills.py --check 2>&1 | head -5
```

Output started with all `planning-with-files` targets `UP-TO-DATE`.

Blocking finding: `.unison/pipelines/p0.yaml`, `p1.yaml`, and `p3.yaml` are invalid YAML due unescaped nested double quotes in `project.test_command`, so those pipeline definitions cannot parse. I did not modify `src/` or `sync-skills.py`.
diff --git a/reviews/iter-1.md b/reviews/iter-1.md
new file mode 100644
index 0000000000000000000000000000000000000000..8a4b163bed97fd2a3ed0da7d56bd34a5fe1243d2
--- /dev/null
+++ b/reviews/iter-1.md
@@ -0,0 +1,23 @@
+---
+verdict: REQUEST_CHANGES
+summary: "Requested check passes, but committed pipeline YAML files are unparsable."
+findings:
+  - severity: blocker
+    category: correctness
+    summary: ".unison/pipelines/p0.yaml, p1.yaml, and p3.yaml have unescaped nested double quotes in project.test_command, so PyYAML cannot parse those pipeline definitions."
+    evidence:
+      - ".unison/pipelines/p0.yaml:8"
+      - ".unison/pipelines/p1.yaml:8"
+      - ".unison/pipelines/p3.yaml:8"
+metrics:
+  test_command: "python3 sync-skills.py --check 2>&1 | head -5"
+  test_exit_code: 0
+  test_output:
+    - "planning-with-files:"
+    - "  hermes: UP-TO-DATE"
+    - "  claude: UP-TO-DATE"
+    - "  codex: UP-TO-DATE"
+    - "  openclaw: UP-TO-DATE"
+  additional_checks:
+    pipeline_yaml_parse: "3 failed, 1 passed"
+---

tokens used
29,760
Wrote the review to [reviews/iter-1.md](/home/sean/shared-skills/reviews/iter-1.md).

Verdict: `REQUEST_CHANGES`

Requested test passed:

```bash
python3 sync-skills.py --check 2>&1 | head -5
```

Output started with all `planning-with-files` targets `UP-TO-DATE`.

Blocking finding: `.unison/pipelines/p0.yaml`, `p1.yaml`, and `p3.yaml` are invalid YAML due unescaped nested double quotes in `project.test_command`, so those pipeline definitions cannot parse. I did not modify `src/` or `sync-skills.py`.
